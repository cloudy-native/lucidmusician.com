---
import Page from "./Page.astro";

interface Props {
  title: string;
  description: string;
  showCta?: boolean;
  ogType?: "website" | "article";
  articlePublishedTime?: string;
  articleModifiedTime?: string;
  structuredData?: Record<string, unknown> | Record<string, unknown>[];
}

const {
  title,
  description,
  showCta = false,
  ogType,
  articlePublishedTime,
  articleModifiedTime,
  structuredData,
} = Astro.props;
const fullTitle = `${title} | LucidHarmony`;
---

<Page
  title={fullTitle}
  description={description}
  ogType={ogType}
  articlePublishedTime={articlePublishedTime}
  articleModifiedTime={articleModifiedTime}
  structuredData={structuredData}
>
  <div class="py-10 w-full min-w-0">
    <div class="text-center mb-12">
      <h1 class="text-3xl md:text-4xl font-bold break-words" style="color: var(--color-text);">{title}</h1>
      <p class="text-base md:text-lg mt-4 max-w-3xl mx-auto" style="color: var(--color-text-muted);">{description}</p>
    </div>

    {Astro.slots.has("prepend") && (
      <slot name="prepend" />
    )}

    <article class="prose prose-lg max-w-4xl mx-auto overflow-x-hidden [&>*]:max-w-full">
      <slot />
    </article>

    {Astro.slots.has("append") && (
      <slot name="append" />
    )}
  </div>

  {showCta && (
    <div class="mt-12 mb-16 max-w-4xl mx-auto rounded-2xl p-8 text-center" style="border: 1px solid var(--color-border); background-color: var(--color-bg-card);">
      <h3 class="text-xl font-bold mb-3" style="color: var(--color-text);">Try LucidHarmony</h3>
      <p class="mb-5 max-w-lg mx-auto" style="color: var(--color-text-muted);">
        Gorgeous, infinite chord progressions for ambient and scoring — powered by the AI techniques discussed in this article.
      </p>
      <div class="flex flex-wrap items-center justify-center gap-4">
        <a
          href="https://lucidmusician.gumroad.com/l/lucid-harmony"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center px-6 py-3 rounded-full text-white font-semibold shadow-lg hover:shadow-xl transition-shadow"
          style="background-color: var(--color-button-bg);"
        >
          Get LucidHarmony
        </a>
        <a href="/docs" class="inline-flex items-center px-6 py-3 rounded-full font-semibold transition-colors" style="border: 1px solid var(--color-border); color: var(--color-text-muted);">
          Read the Docs
        </a>
        <a href="/blog" class="inline-flex items-center px-6 py-3 rounded-full font-semibold transition-colors" style="border: 1px solid var(--color-border); color: var(--color-text-muted);">
          More Articles
        </a>
      </div>
    </div>
  )}
</Page>
